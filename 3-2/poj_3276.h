#pragma 
#include <algorithm>
#include <map>
#include <set>
#include <vector>

namespace poj_3276{

    void solve(std::string y, int& m, int& k){

        k = 1;
        m = y.size();

        for(int i = 0; i < y.size(); i++){

            
        }
    }
}